package ${package_name}.api;

import com.camile.common.base.ServiceMock;
import ${package_name}.dao.mapper.${model}Mapper;
import ${package_name}.dao.model.${model};
import ${package_name}.dao.model.${model}Example;

/**
* 降级实现${model}Service接口
* Created by lizhihui on ${ctime}.
*/
public class ${model}ServiceMock extends ServiceMock<${model}Mapper, ${model}, ${model}Example> implements ${model}Service {

}
